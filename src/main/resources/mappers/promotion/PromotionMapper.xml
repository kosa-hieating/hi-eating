<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.or.hieating.promotion.mapper.PromotionMapper">
  <select id="findActivePromotions" resultType="Promotions">
    SELECT
      id,
      title,
      img_src AS imgSrc,
      link,
      display_order AS displayOrder,
      starts_at AS startsAt,
      ends_at AS endsAt,
      created_at AS createdAt,
      updated_at AS updatedAt
    FROM promotions
    WHERE SYSTIMESTAMP BETWEEN starts_at AND ends_at
    ORDER BY display_order
  </select>
</mapper>
